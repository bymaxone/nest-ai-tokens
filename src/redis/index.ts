/**
 * @fileoverview Public barrel for the `./redis` subpath — the
 * `RedisBudgetCounterStore` implementation of the budget counter port.
 * Requires the optional `ioredis` peer dependency.
 * @layer redis
 */

export {}
