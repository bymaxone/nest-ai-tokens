/**
 * @fileoverview Public barrel for the `./prisma` subpath — the official
 * `PrismaAiTokensStore` adapter plus its schema fragment and migrations.
 * Requires the optional `@prisma/client` peer dependency.
 * @layer prisma
 */

export {}
